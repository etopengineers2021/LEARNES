/*Divide two number or print if division isn't possible*/
#include<stdio.h>
int main(){
    int a = 15 ,b = 0;
    
    if(b !=0 ) printf("The division a / b= %d",a / b);
    else printf("division isn't possible\n");
}