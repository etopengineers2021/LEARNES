#include<stdio.h>
#include<math.h>
int main(){
    int base = 3;
    int exponent = 5;
    int result = pow(base , exponent);
    printf("The pow 8.0^3:%lf\n",pow(8.0, 3));
    printf("base=3^5 :%d",result);
    return 0;
}