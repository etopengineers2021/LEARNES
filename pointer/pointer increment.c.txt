#include<stdio.h>
int main(){
    int var[]={10,20,30};
    int* ptr=var;
    printf("the conent in first indux%d\n",*ptr);
   
    printf("the content after increment&%d\n", *(ptr++));
      printf("the conent in first fst%d\n",*ptr);
    ++*ptr;
     printf("the content after increment%d\n",*ptr);
     ++*ptr;
     printf("the content after increment%d\n",*ptr);
         *++ptr;
    printf("the content after increment%d\n",*ptr);

}