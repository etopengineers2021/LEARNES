#1. CMAKE VERSION 
cmake_minimum_required(VERSION 3.13) 
# 2.set pico sdk path  
set(PICO_SDK_PATH "C:/Users/ishar/.pico-sdk/sdk/2.2.0")
include(${PICO_SDK_PATH}/external/pico_sdk_import.cmake)
#  3. its like macro we don't need to write the project name all place , just use the key PROJECT_NAME.
set(PROJECT usb_1)    
# set(picotoolVersion 2.2.0-a4)
set(picotool_DIR "C:/Users/ishar/.pico-sdk/picotool/2.2.0-a4/picotool" CACHE PATH "Path to picotool")

# 4.create a project 
project(PROJECT C CXX ASM)  
#5.initialize pico sdk
pico_sdk_init()
# 6.create a executable target 
add_executable(PROJECT   #  that creat program target 
    
    main5.c
)
# 7.link libraries 
target_link_libraries(PROJECT # that link the libeary  
    pico_stdlib
    hardware_uart
  
    pico_multicore
)
 #8. printf/getchar path
pico_enable_stdio_usb(PROJECT 1)
pico_enable_stdio_uart(PROJECT 0)
#9. generate uf2/
pico_add_extra_outputs(PROJECT) #that create  uf2 file  